const express = require("express");
const router = express.Router();
const {  getUser, deleteUser, searchUsers, getUsers } = require("../controllers/notificationsController");

//User
// router.get("/", );




module.exports = router;
